export default async function handler(req,res){
  // Platzhalter – später je Verband spezifizieren (DEU, NED, FRA, AUS, NZL, …)
  // Du kannst pro Nation eigene Fetcher bauen oder Daten von Admin gepflegt speichern.
  res.json([]);
}
